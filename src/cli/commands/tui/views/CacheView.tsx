import React from 'react';
import { Box, Text } from 'ink';
import type { TuiDeps } from '../types.js';
export function CacheView({ deps: _ }: { deps: TuiDeps }) {
  return <Box><Text>Caché — en construcción</Text></Box>;
}
